import 'package:flutter/material.dart';

class PricipalPage extends StatelessWidget {
  const PricipalPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const SafeArea(
        child: Scaffold(
      body: Center(
        child: Text('the best thing is this'),
      ),
    ));
  }
}
